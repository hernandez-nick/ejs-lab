<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= `${restaurant.name} - ${menu[0].category.charAt(0).toUpperCase() + menu[0].category.slice(1)}` %>
    </title>


</head>

<body>

<%- include('./partials/nav') %>
    
    <h1><%= menu[0].category.charAt(0).toUpperCase() + menu[0].category.slice(1) %></h1>

    <% menu.forEach(item => { %>
        <h3><%= item.name %>
        <% if (item.rating===5) { %>⭐<% } %></h3>
        <p>Price: $<%= item.price %></p>
        <p>Rating: <%= item.rating %>/5</p>
        <p><%= item.details %></p>
    <% }) %>

</body>